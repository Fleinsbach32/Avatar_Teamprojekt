# Dev-Log: 16.06.2026

## Übersicht

Großer Arbeitstag mit vier geplanten Teil-Projekten (TP1–TP4), einem
diagnostizierten externen Fehler (geleakter Google-API-Key) und der
Wieder-Einführung der Web-Authentifizierung mit korrigiertem Zuschnitt.

Methodik: strukturiertes Brainstorming → Design-Spec → Umsetzung (TDD) je
Teil-Projekt. Reihenfolge auf Wunsch: 1 → 4 → 2 → 3.

Tests am Ende des Tages: **84/84 grün.**

---

## 1. Merge des Team-Stands + Aufräumen davor

- Auth-Entfernung aus dem vorigen Stand (blockierte den Tavus-Voice-Pfad) und
  Angleichung der RAG-Trunkierungstests auf 600 Zeichen committet.
- `git pull` brachte drei Team-Commits: Projekt-Umstrukturierung
  (`data/pdfs/`, `scripts/fill_db.py`, `docs/DOKUMENTATION.md`, FAQ in
  `faq_de.json`/`faq_eng.json`), einen Web-Crawler und HTTP Basic Auth.

## 2. Diagnose: Avatar bricht bei Spracheingabe ab

Jeder `/chat`-Aufruf lieferte „Service momentan nicht verfügbar." Per Reproduktion
des exakten Gemini-Aufrufs gefunden:
```
403 PERMISSION_DENIED: "Your API key was reported as leaked. Please use another API key."
```
Kein Code-Bug — der Google-API-Key wurde von Google gesperrt, weil er früher in
der Git-Historie (`.env` eingecheckt, Commits 4339100f/662a51b5/c1fa7e9b)
veröffentlicht wurde. **Offene Aufgabe:** Key rotieren, neuen in `.env` (nicht
committen, ist gitignored).

## 3. TP1 — Aufräumen & Struktur

- `crawler.py` + `crawl.ps1` → `scripts/` (Pfade auf Projekt-Root angepasst).
- `chroma_db.zip` und 15 byte-identische `data/`-Wurzel-PDFs gelöscht
  (`data/pdfs/` ist einzige Quelle).
- **Bugfix:** `fill_db.py` las noch `data/faq.json` (gelöscht) → lädt jetzt
  `faq_de.json` + `faq_eng.json` robust (leere Dateien werden übersprungen;
  `faq_eng.json` ist aktuell leer), Pfade `PROJECT_ROOT`-basiert.
- Neuer Test `tests/test_fill_db_paths.py`.

## 4. TP4 — Avatar-Beenden-Button

- Roter Overlay-Button oben links auf dem Avatar-Video; `endAvatar()` ruft
  `tavusCall.leave()` → bestehender `left-meeting`-Teardown übernimmt alles.
- i18n (DE „⏹ Beenden" / EN „⏹ End"), Ein-/Ausblenden an Start/Trennung/Fehler.
- Reines Frontend, kein Backend-Change.

## 5. TP2 — RAG, Modulnummern & Studiengang-Zugriff

Befunde: Modul-IDs liegen als `M-WIWI-…` **und** `T-…` vor (Regex erfasste nur
`M-`); Detailsektionen beginnen konsistent mit `Modul: <Name> [ID]`; das
Zeichen-Chunking zerschnitt Name ↔ ID; der Studiengang-Filter war auf ein PDF
beschränkt; Umlaut-Extraktion ist korrekt (Fehlalarm der Konsole).

Umsetzung:
- `fill_db.py`: **modulweises Chunking** (`chunk_handbook`) — Name + ID bleiben
  pro Chunk zusammen; einheitliche Metadaten
  (`doc_type`/`program`/`module_id`/`module_name`); sauberer Rebuild
  (Collection wird verworfen & neu angelegt); Ausführung in `main()` gekapselt.
- `rag.py`: Regex `M-`/`T-`; Studiengang-Filter via
  `where={"$or": [{"program": key}, {"program": "all"}]}` (Handbuch + FAQ/
  Allgemeines, andere Handbücher aus); exakter `module_id`-Metadaten-Lookup mit
  Volltext-Fallback.
- `prompts.py`: Modulnummern werden **nur auf ausdrückliche Nachfrage** genannt.
- ChromaDB neu aufgebaut: **12.098 Einträge**; Retrieval auf echten Daten
  verifiziert (Modul-ID-Lookup, Studiengang-Filter) — ohne Gemini.

## 6. TP3 — Crawler-Review (autonom umgesetzt)

- **Metadaten an TP2 angeglichen:** `program: "all"` in allen Ingestion-Pfaden,
  sonst wären gecrawlte Inhalte im Studiengang-Filter unsichtbar.
- **Bugfix `is_internal`:** `lstrip("www.")` entfernte Zeichen statt Präfix
  (`www.wiwi.kit.edu` → `iwi.kit.edu`) → `_strip_www`-Helfer.
- `ALLOWED_DOMAINS` um fachschaft.org, hoc/zak/sle.kit.edu erweitert (breite
  `www.kit.edu` bewusst seed-only).
- Distanzmetrik vereinheitlicht (cosine-Override entfernt).
- `tests/test_crawler.py` (7 Tests).

## 7. Web-Authentifizierung wieder eingeführt (korrigiert)

Die Team-Variante hängte Basic Auth an **alle** Router — auch an die von Tavus
serverseitig aufgerufenen LLM-Endpoints → 401 → Avatar-Abbruch. Neu:
- `app/auth.py` mit `check_auth` (HTTP Basic, `APP_USERNAME`/`APP_PASSWORD`).
- Geschützt: `/`, `/chat`, `/avatar/config`, `/tavus/session|end|message|settings`.
- **Bewusst offen:** `/tavus/llm`, `/tavus/llm/chat/completions`,
  `/chat/completions` (Tavus kann keine Credentials senden), `/health`.
- Tests umgehen Auth per `dependency_overrides` (conftest), plus `tests/test_auth.py`
  prüft 401 ohne Login, offenen LLM-Endpoint und akzeptierte Credentials.

## 8. Offene Punkte

- **Google-API-Key rotieren** und `run.ps1` neu starten (neuer Key + neue
  Collection). Danach End-to-End-Qualitätscheck TP2 + Avatar-Beenden live.
- `faq_eng.json` ist leer → bei Bedarf englische FAQs befüllen.
- Crawl-Workflow (`scripts/crawl.ps1`) ist optional und separat; `run.ps1` baut
  nur PDF/FAQ-Basis auf.
- Alter Google-API-Key bleibt in der Git-Historie (tot, harmlos); History-Rewrite
  nur abgestimmt im Team.

## 9. Git

Branch `feature/tavus`. Heutige Commits u.a.:
- `revert: remove HTTP Basic Auth …; align RAG truncation tests to 600`
- `docs: TP1/TP2/TP3/TP4 design specs`
- `refactor(tp1): move crawler to scripts/, fix fill_db FAQ paths, tidy root`
- `feat(tp4): add avatar end button …`
- `feat(tp2): module-wise handbook chunking, program-based studiengang filter, …`
- `fix(tp3): crawler – program metadata, is_internal www bug, allowed domains, …`
- `feat(auth): re-add HTTP Basic Auth for browser endpoints (Tavus LLM exempt)`

---

## Anhang: Detailnotizen zu den frühen Team-Commits (783e2df8, 0fce9e05)

> Diese Notizen beschreiben die beiden frühen Commits des Tages. Der dort
> beschriebene Auth-Zuschnitt (alle Routen inkl. `/tavus/llm`) wurde später in
> dieser Session überarbeitet (siehe Abschnitt 7: LLM-Endpoints ausgenommen).
> Ebenso wurden die unten genannten offenen Punkte (Chunk-Größe in `fill_db.py`,
> Studiengang-Filter) in TP2/TP3 adressiert.

Vier Themenbereiche: Prompt-Qualität verbessert (Wissensgrenzen, Formatregeln),
RAG-Retrieval robuster gemacht (Modul-ID-Direktsuche, Relevanzcheck), Web-Crawler
für wiwi.kit.edu hinzugefügt, und HTTP Basic Auth für den Webserver-Betrieb
eingebaut.

Commits heute (2):
- `feat: add web crawler, fix RAG quality and prompt rules` (783e2df8)
- `feat: add HTTP Basic Auth for web server deployment` (0fce9e05)

---

## 1. Prompt-Qualität (`app/prompts.py`)

### 1.1 Neuer Abschnitt „Wissensgrenzen" im Base-Prompt

Das größte Problem: KIRA antwortete auf gültige Fragen (Bewerbung, NC, Einschreibung)
mit „Dafür bin ich leider nicht zuständig" — weil die einzige Fallback-Formulierung
für beides galt: nicht-zuständig UND nicht-wissend. Fix: Explizite Regel, die beide
Fälle trennt.

> „Nicht zuständig" → ausschließlich für Themen ohne Bezug zu Studium/KIT.
> Wissenslücke (NC, Fristen, Moduldetails) → ehrlich zugeben + campus.kit.edu.

Außerdem: Wenn eine Modulnummer genannt wird (z.B. M-WIWI-101267), erst in der
Wissensbasis suchen und mit Klarname antworten. Nur falls nichts gefunden, nach
dem Namen fragen.

### 1.2 Antwortregeln verschärft

- „Antworte kurz und präzise" explizit in Persönlichkeit und Antwortregeln.
- Verboten: Floskeln wie „Ich verstehe, dass du wissen möchtest...", „Das ist eine
  gute Frage", „Natürlich". KIRA kommt direkt zum Punkt.
- Verboten: Markdown-Formatierung (*kursiv*, **fett**) — stört die Sprachausgabe.
- Bisherige separate „Länge"-Sektion in Antwortregeln integriert (war redundant).
- Voice-Prompts (DE+EN) ebenfalls um No-Markdown- und No-Floskel-Regel ergänzt.
- Englische Version vollständig synchronisiert.

### 1.3 Tavus _VOICE_CONTEXT verbessert (`app/routes/tavus.py`)

Der kurze Inline-Kontext, den Tavus beim Session-Start erhält, wurde um du-Form
und Keine-Listen-Regel ergänzt.

---

## 2. RAG-Verbesserungen (`app/rag.py`)

### 2.1 Modul-ID-Direktsuche

**Problem:** Semantische Embeddings taugen nicht für IDs wie „M-WIWI-101267" —
das Modell findet keinen sinnvollen Nachbarn.

**Fix:** Regex `M-[A-Z]+-\d+` erkennt eine Modul-ID in der Frage. Bei Treffer
läuft zuerst eine `where_document={"$contains": module_id}`-Query (Volltext-Match),
bevor die normale semantische Suche startet. Liefert der Volltext-Match Ergebnisse,
werden sie mit einer eigenen Anweisung ans Modell weitergegeben: „Antworte auf
Basis dieses Kontexts, Klarname ohne ID."

### 2.2 Robusterer n_results-Fallback

**Problem:** Wenn ein Studiengang-Filter aktiv ist und die Collection sehr wenige
passende Chunks hat, konnte ChromaDB bei `n_results=3` einen Fehler werfen.

**Fix:** Neue Hilfsfunktion `_query_safe()` fängt den Fehler ab und wiederholt
die Query mit `n_results=1`. Schlägt auch das fehl, wird ein leeres Ergebnis
zurückgegeben.

### 2.3 Chunk-Größe erhöht

`doc[:400]` → `doc[:600]` pro Chunk in der Kontext-Zusammensetzung. Gibt dem
Modell mehr Kontext je Treffer, besonders bei längeren Modulbeschreibungen.

### 2.4 RAG-Anweisung flexibler

Statt „ausschließlich auf Basis des Kontexts" sagt die Anweisung bei gutem Treffer
jetzt: „Nutze den Kontext wenn er zur Frage passt, sonst ignoriere ihn und nutze
allgemeines Hochschulwissen." Verhindert, dass KIRA bei falsch retrievten Chunks
(z.B. Vollmacht statt Einschreibung) trotzdem den falschen Inhalt wiedergibt.

---

## 3. Web-Crawler (`crawler.py` + `crawl.ps1`)

### 3.1 Crawler

BFS-Crawler (Breiten-Suche) speziell für wiwi.kit.edu plus externe Seeds
(Fachschaft, HOC, ZAK, SLE, KIT.edu). Funktionen:

- **robots.txt**-Compliance (gecacht pro Domain)
- **Shibboleth-Erkennung:** Login-Redirects werden erkannt und übersprungen
- **Stream-Download:** Response-Headers werden vor dem Body geprüft — binäre
  Content-Types und Dateien > 5 MB nie heruntergeladen
- **URL-Blacklist:** PHP-Skripte die immer hinter Login liegen
- **Sentence-aware Chunking:** Chunks bis 300 Tokens, 30 Token Überlappung,
  Satzgrenzen werden respektiert
- **Spracherkennung** (langdetect, optional)
- **PDF-Extraktion** via PyMuPDF (fitz, optional)
- **ChromaDB-Ingestion:** direkt beim Crawl (`--embed`) oder nachgelagert
  (`--fill-db`)
- **Modul-Bewertungen** (`--inject-ratings`): strukturierte Bewertungsdaten aus
  `module_ratings.json` als Chunks in ChromaDB

Warum bricht der Crawler bei wiwi.kit.edu nach ~284 Seiten ab: `ALLOWED_DOMAINS`
enthält nur wiwi.kit.edu — Links von externen Seeds werden nicht verfolgt.
Außerdem ist `max_depth=3` der Standard — depth=3-Seiten erzeugen keine Kinder.
Beides ist bewusst so konfiguriert; wiwi.kit.edu hat schlicht nicht mehr als
~280 öffentlich erreichbare Seiten.

### 3.2 crawl.ps1

Einzeiliges Setup-Skript mit denselben Stamp-Mechanismus wie `run.ps1`:

```
.\crawl.ps1                  # Standard: pip → crawl → fill-db
.\crawl.ps1 -MaxPages 200    # Schneller Testlauf
.\crawl.ps1 -FillDbOnly      # Nur JSONs → ChromaDB einbetten
.\crawl.ps1 -InjectRatings   # Nur Modulbewertungen einpflegen
```

Enthält automatische UTF-16-Erkennung für `requirements.txt` (BOM `FF FE`
→ re-encode als UTF-8 ohne BOM) — notwendig, weil das Write-Tool auf Windows
Dateien im UTF-16-Format speichert, pip damit aber nicht umgehen kann.

### 3.3 requirements.txt

- Encoding repariert: UTF-16 LE mit BOM → UTF-8 ohne BOM (via PowerShell
  `[System.IO.File]::WriteAllText` mit `UTF8Encoding($false)`)
- Neu: `beautifulsoup4>=4.12`, `PyMuPDF>=1.24`, `langdetect>=1.0.9`

---

## 4. HTTP Basic Auth (`app/main.py`)

Für den Webserver-Betrieb: alle Routen außer `/health` erfordern jetzt
HTTP-Basic-Authentifizierung. Credentials kommen aus `APP_USERNAME` /
`APP_PASSWORD` (Env-Variablen, Default `admin` / `geheim`).

```python
def check_auth(credentials: HTTPBasicCredentials = Depends(security)):
    # secrets.compare_digest verhindert Timing-Angriffe
```

`/health` bleibt ohne Auth (Monitoring/Loadbalancer).

**Wichtig für Tavus-Voice-Pfad:** `/tavus/llm` wird von Tavus-Servern aufgerufen.
HTTP Basic Auth greift auch dort — die Custom-LLM-URL im Tavus-Dashboard muss
die Credentials enthalten: `https://user:passwort@domain.de/tavus/llm`.

`.env.example` um `APP_USERNAME` und `APP_PASSWORD` ergänzt.

---

## 5. .gitignore

`crawled_data/` (JSON-Output des Crawlers) und `.pip-stamp` ignoriert.

---

## 6. Bekannte Offene Punkte

- **Orientierungspraktikum / Orientierungsprüfungen:** Die MHBs enthalten diese
  Infos, aber die Chunk-Größe (300 Zeichen in `fill_db.py`) zerschneidet die
  relevanten Passagen. Entweder `fill_db.py` auf größere Chunks umstellen oder
  die MHBs neu einpflegen.
- **Studiengang-Dropdown kein sichtbarer Effekt bei Verwaltungsfragen:** Der
  Filter trifft nur auf Modul-Abfragen. Für Einschreibung/Bewerbung/NC sind
  keine Dokumente in der Wissensbasis — KIRA fällt immer auf LLM-Wissen zurück.
  Lösung: studiengangs-spezifische FAQ/SPO-Dokumente in ChromaDB einpflegen.
- **Tavus-Auth:** Credentials in der Tavus-Dashboard-URL müssen manuell gesetzt
  werden (kein automatischer Ablauf).
- **Voice global state** (aus Vortag): Für parallele Sessions Redis einsetzen.
