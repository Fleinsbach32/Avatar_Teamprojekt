# Dev-Log: 2026-06-22 Session 2 — Optimization Pass

## DB-Reparatur
- Zwei ChromaDB-Verzeichnisse identifiziert: `chroma_db/` (67.112 Einträge, voll) und `data/chroma_db/` (leer, durch Pfadänderung in Commit c12e9c6b entstanden)
- `rag.py`, `fill_db.py`, `inspect_db.py` auf `chroma_db/` (Root) vereinheitlicht
- Volle DB als `data/chroma_db.zip` (1,15 GB) gesichert, leeres `data/chroma_db/` entfernt
- `crawl.ps1` war versehentlich gelöscht und wiederhergestellt; Root-Wrapper ist dünn und leitet an `scripts/crawl.ps1` weiter
- Neues Skript `scripts/check_db.py`: zeigt Eintragsanzahl, Typ-Verteilung, Studiengänge, Sanity Checks

## RAG-Filter-Fix
- `_WHAT_IS_MODULE_RE` um optionales "Modul"-Keyword erweitert — fängt jetzt "Was ist Introduction to Digital Economics?" (zuvor unerkannt)
- Ohne Studiengang-Filter kein "nicht in DB"-Fallback mehr; stattdessen Standard-Semantiksuche
- `_merge_handbook_priority()` hinzugefügt: garantiert ≥3 studiengangs-spezifische Handbuch-Chunks im finalen Kontext, auch wenn CrossEncoder generische Web-Chunks höher rankt

## Latenz-Optimierung (Workstream A)
- CrossEncoder-Reranker im Lifespan vorgeladen (Warmup-Predict)
- Kandidatenpool reduziert: 8+4=12 (Pflicht: 12+4=16), rerankt auf top-6 (vorher 10+6=16–21)
- Neues Skript `scripts/bench_rag.py`: misst Latenz mit/ohne Reranker pro Query-Typ

## Prompts (Workstream D)
- Moderater empathischer Ton ergänzt: "ermutige, ohne zu beschönigen" / "encourage, without sugar-coating" in allen vier Prompt-Varianten (DE/EN × Text/Voice)
- Voice und Text strukturell angeglichen

## UI (Workstreams C, E)
- Dropdown-Labels eindeutig: "WING (B.Sc.)" / "WING (M.Sc.)" statt beides "WING"
- Sprach-UI: Single-Toggle ersetzt durch zwei Flaggen-Buttons DE | EN mit aktiver Markierung

## Hygiene & Code-Review (Workstream F)
- Chat-Retry: bis zu 3 Versuche für Gemini-Stream vor erstem Chunk; kein Retry nach erstem gesendeten Chunk
- Message-Längenlimit: 2000 Zeichen in `/chat` und `/tavus/message`
- Tavus-Logging: API-Erfolg (200/201) auf `info`; Fehler auf `warning`
- `voice_openings`-Cleanup war bereits in `session.py` implementiert

## Offen (dokumentiert, nicht in diesem Pass)
- Webcrawl-`source`-Metadatum: nur 1 unique URL / leere Domain — Datenqualität bei `fill_db.py`, separater Task
- `is_pflicht`-Metadatum für vollständige Pflichtmodul-Listen — strukturell, separater Task
