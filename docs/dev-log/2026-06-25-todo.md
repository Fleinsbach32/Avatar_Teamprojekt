# TODO: 2026-06-25

Stand Vortag: Block 1–3A + Gemini-Resilienz + Doku abgeschlossen, 143 Tests grün,
alles auf `origin/feature/tavus` gepusht (Stand `78e82c70`).

---

## ✅ Erledigt am 2026-06-24 (Nachtrag)

- **Flash-vs-flash-lite-Vergleich abgeschlossen.** flash sauber (0/20 Ausfälle, TTFT 529 ms);
  flash-lite in dieser Umgebung wiederholt gedrosselt (7/20, TTFT 7326 ms). Qualität von
  flash-lite hielt — Problem ist Verfügbarkeit, nicht Qualität.
  **Entscheidung: bei `gemini-2.5-flash` bleiben. Block 3 (Latenz) faktisch abgeschlossen.**

## 1. Deployment (unabhängig von Gemini)

- [ ] **Railway-Deploy verifizieren**: Volume `/data`, `CHROMA_DB_DIR=/data/chroma_db`,
  `CHROMA_DRIVE_FILE_ID=<Drive-ID>` gesetzt? Bootstrap beim ersten Deploy prüfen
  (Log: „ChromaDB erfolgreich … entpackt").

## 2. Verbesserungen (priorisiert, aus Code-Review 2026-06-24)

- [ ] **#3 Google-API-Key rotieren** — liegt in der Git-Historie. Echtes Sicherheitsthema. (schnell)
- [ ] **#2 CI-Pipeline** — GitHub-Actions-Workflow, der `pytest tests/` bei jedem Push laufen lässt. Sichert die 143 Tests + alle Qualitätsgewinne ab. (eigener kleiner Block)
- [ ] **#1 Generierungs-Eval** — größter Qualitätshebel: misst, ob KIRAs *tatsächliche Antwort* korrekt ist (nicht nur Retrieval). LLM-as-Judge auf `--show-answers`-Output. **Eigenes Brainstorming wert.**
- [ ] **Fallback-Modell überdenken** — flash-lite wird für diesen API-Key konsequent gedrosselt; als 503-Fallback (flash → flash-lite) daher schwach. Alternatives Fallback-Modell prüfen oder Fallback ganz weglassen.
- [ ] **#4 Gemini Context-Caching** — einziger noch offener Latenz-/Kostenhebel (System-Prompt-Prefix cachen). ~50–150 ms TTFT + günstiger. (optional, da flash schon „gut genug")
- [ ] **#5 `bench_rag.py` in `bench_latency.py` integrieren** — zwei ähnliche Benchmark-Skripte zusammenführen (Reranker-an/aus als Sub-Modus). (optional)

## Referenz

- **Latenz abgeschlossen:** ~98 % Gemini, RAG ~18 ms. flash sauber bei TTFT ~529 ms / e2e ~1,1 s.
  flash-lite in dieser Umgebung gedrosselt → nicht nutzbar. Entscheidung: bei flash bleiben.
- Modellwahl: flash bleibt Primär (FACTS-Grounding 85,3 % vs Gemini 3 Flash 61,9 %).
