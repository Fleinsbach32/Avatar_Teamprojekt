# TODO: 2026-06-25

Stand Vortag: Block 1–3A + Gemini-Resilienz + Doku abgeschlossen, 143 Tests grün,
alles auf `origin/feature/tavus` gepusht (Stand `78e82c70`).

---

## 1. Blockiert durch instabiles Gemini (zuerst, wenn Gemini sauber läuft)

- [ ] **Flash-vs-flash-lite-Vergleich** (Plan: `docs/superpowers/plans/2026-06-24-flash-lite-test.md`, Task 3)
  - Back-to-back, beide Modelle explizit:
    ```
    python scripts/bench_latency.py --model gemini-2.5-flash      --save bench_flash.json --show-answers
    python scripts/bench_latency.py --model gemini-2.5-flash-lite --save bench_lite.json  --show-answers
    ```
  - **Lauf nur gültig wenn:** Fehlschläge ~0 UND TTFT-Median < ~800 ms (sonst Gemini überlastet → verwerfen).
  - Auswerten: Latenz-Delta + Antwortqualität (Modul-/Fakten-Fragen) Seite an Seite.
  - **Entscheidung:** flash-lite spürbar schneller UND Qualität hält → Voice-Pfad (`app/routes/tavus.py`) auf flash-lite umstellen (eigener Block). Sonst bei flash bleiben.
- [ ] **Sauberer Latenz-Re-Baseline** (`bench_baseline.json` war im Ausfall gemessen, unbrauchbar).

## 2. Deployment (unabhängig von Gemini)

- [ ] **Railway-Deploy verifizieren**: Volume `/data`, `CHROMA_DB_DIR=/data/chroma_db`,
  `CHROMA_DRIVE_FILE_ID=<Drive-ID>` gesetzt? Bootstrap beim ersten Deploy prüfen
  (Log: „ChromaDB erfolgreich … entpackt").

## 3. Verbesserungen (priorisiert, aus Code-Review 2026-06-24)

- [ ] **#3 Google-API-Key rotieren** — liegt in der Git-Historie. Echtes Sicherheitsthema. (schnell)
- [ ] **#2 CI-Pipeline** — GitHub-Actions-Workflow, der `pytest tests/` bei jedem Push laufen lässt. Sichert die 143 Tests + alle Qualitätsgewinne ab. (eigener kleiner Block)
- [ ] **#1 Generierungs-Eval** — größter Qualitätshebel: misst, ob KIRAs *tatsächliche Antwort* korrekt ist (nicht nur Retrieval). LLM-as-Judge auf `--show-answers`-Output. **Eigenes Brainstorming wert.**
- [ ] **#4 Gemini Context-Caching** — einziger echter ungenutzter Latenz-/Kostenhebel (System-Prompt-Prefix cachen). Teil von Block 3B.
- [ ] **#5 `bench_rag.py` in `bench_latency.py` integrieren** — zwei ähnliche Benchmark-Skripte zusammenführen (Reranker-an/aus als Sub-Modus). (optional)

## Referenz

- Latenz-Befund: ~98 % Gemini, RAG ~18 ms. Risikoarme Hebel bringen nur ~100 ms;
  großer Hebel = flash-lite Voice (Qualitäts-Tradeoff) → siehe Punkt 1.
- Modellwahl: flash bleibt Primär (FACTS-Grounding 85,3 % vs Gemini 3 Flash 61,9 %).
